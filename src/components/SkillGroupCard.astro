---
import type { SkillGroup } from "../interfaces/SkillGroup";
interface Props extends SkillGroup {}

const { icon: Icon, name, technologies } = Astro.props;
---

<div
  class="flex flex-col bg-base-200 rounded-lg border-base-300 overflow-hidden transition-all duration-300 hover:-translate-y-1 shadow-sm hover:shadow-md"
>
  <div
    class="text-base sm:text-lg md:text-xl font-semibold bg-[#5C6370] p-3 sm:p-3 md:p-4 items-center gap-x-2"
  >
    <div class="flex flex-row items-center gap-x-2 ml-1 sm:ml-1 md:ml-2">
      <Icon class="size-7 sm:size-8 md:size-10" />
      <span class="text-lg sm:text-xl md:text-2xl font-bold text-white">{name}</span>
    </div>
  </div>
  <div class="p-3 sm:p-3 md:p-4 grid grid-cols-2 sm:grid-cols-3 gap-2 sm:gap-3 md:gap-4">
    {
      technologies.map((tech) => {
        const TechIcon = tech.icon;
        return (
          <a 
            href={tech.url} 
            class="flex flex-col justify-center items-center transform hover:scale-110 transition-transform p-1 sm:p-2 rounded-lg hover:bg-base-300/20" 
            target="_blank" 
            rel="noopener noreferrer"
            title={tech.name}
          >
            <TechIcon class="size-8 sm:size-10 md:size-12" />
            <span class="mt-1 sm:mt-1 md:mt-2 text-center font-medium text-xs sm:text-xs md:text-xs leading-tight px-1">{tech.name}</span>
          </a>
        );
      })
    }
  </div>
</div>
