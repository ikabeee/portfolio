---
const { label, icon: Icon, size, type, title } = Astro.props;
---

<button
  type={type}
  title={title}
  class={`btn btn-${size} flex gap-2 bg-[#61AFEF] text-white items-center`}
>
  {label}
  <Icon class="size-5 md:size-6 fill-white" />
</button>
