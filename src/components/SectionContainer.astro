---
const { id, title } = Astro.props;
---
<section id={id} data-selection={id} class=`section scroll-m-20 mx-auto lg:max-w-6xl md:max-w-2xl`>
    {title && (<h2 class="text-3xl font-bold mb-4">{title}</h2>)}
    <slot/>
</section>